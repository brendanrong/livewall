# Build outputs (regenerable via build.sh / make_dmg.sh / notarize.sh)
LiveWall.app/
LiveWall.dmg
LiveWall-*.dmg

# Secrets — never commit these
Sources/Generated/
.livewall-secrets

# macOS clutter
.DS_Store

# Editor / OS clutter
*.swp
*.swo
.idea/
.vscode/

# Xcode (if anyone ever opens this in Xcode)
*.xcuserstate
xcuserdata/
DerivedData/
.build/
Package.resolved
